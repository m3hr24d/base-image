#!/usr/bin/env bash
set -eu

USAGE='Usage:\nexec-as-user USERNAME COMMAND';
USERNAME=${1:-}; shift
COMMAND=${@:-}

if [[ "$#" -lt 1 ]]; then
    echo -e ${USAGE}
    exit 1
fi

if [[ $(whoami) == ${USERNAME} ]]; then
        $@
    else
        runuser --login ${USERNAME} --command "$COMMAND"
fi
